import types from '../actions/types';

const DAFAULT_STATE = {

};

export default (state = DAFAULT_STATE, action) => {
    switch(action.type){
        default:
        return state;
    }
}